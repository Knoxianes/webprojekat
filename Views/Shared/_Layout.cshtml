<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/Content/Site.css"/>
    <title>Web Prodavnica</title>
    
</head>
<body>
    <script src="~/Scripts/jquery/jquery.js">
    </script>
    <div class="navbar">
        <div class="logo-div"><div id="logo">Logo/Home</div></div>
        <div id="profile"></div>
    </div>
    <hr class="navbar-divider" />
    @RenderBody()
    <script>
        let logedIn;
        $(function () {
            logedIn = sessionStorage.getItem("logedIn");
            if (logedIn) {
                if (logedIn == 0) {
                    $("#profile").prepend("K")
                } else if (logedIn == 1) {
                    $("#profile").prepend("P")
                } else if (logedIn == 2) {
                    $("#profile").prepend("A")
                }
            }
        });
        $("#profile").on("click", function () {
            if (logedIn == 0)
            {
                document.location.href = "Kupac";
            }
            else if (logedIn == 1)
            {
                document.location.href = "Prodavac";
            }
            else if (logedIn == 2) {
                document.location.href = "Admin";
            }
            else
            {
                document.location.href = "LogIn";
            }
        });
        $("#logo").on("click", function () {
            document.location.href = "Home";
        });
    </script>
</body>
</html>